# This project currently has no third-party Python dependencies.
# It runs on the Python standard library only.
#
# If a package is added later, list it here one per line, for example:
# requests==2.31.0
